<%= form_for(@park, :html => { class: 'blue-theme' }) do |f| %>
  <% if @park.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@park.errors.count, "error") %> prohibited this park from being saved:</h2>

      <ul>
      <% @park.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: 'control-label' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :website, class: 'control-label' %>
    <%= f.text_field :website, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :user_id, class: 'control-label' %>
    <%= f.select :user_id, User.parks.map { |user| [user.profile.name, user.id] }, { include_blank: false }, { class: 'form-control' } %>
  </div>
  <div class="form-group">
    <%= f.label :aboutme, class: 'control-label' %>
    <%= f.text_area :aboutme, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
