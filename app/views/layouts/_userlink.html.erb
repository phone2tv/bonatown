<ul class="nav navbar-nav navbar-right">
  <% if user_signed_in? %>
    <li class="dropdown">
    <a href=<%= user_path(current_user) %> class="dropdown-toggle" data-toggle="dropdown" data-no-turbolink="true"><%= current_user.email %> <b class="caret"></b></a>
    <ul class="dropdown-menu" role="menu">
      <li><%= link_to current_user.email, current_user, 'data-no-turbolink' => true %></li>
      <li class="divider"></li>
      <li class="dropdown-header">Nav header</li>
      <li><%= link_to t('devise.logout'), destroy_user_session_path, method: :delete %></li>
      <li><%= link_to t('devise.logoff'), registration_path(current_user), :method => :delete, data: { confirm: 'Are you sure?' } %></li>
    </ul>
    </li>
  <% else %>
    <% if controller_name == 'sessions' %>
      <li class="active"><%= link_to t('devise.login'), new_user_session_path, class: 'menulink' %></li>
    <% else %>
      <li><%= link_to t('devise.login'), new_user_session_path, class: 'menulink' %></li>
    <% end %>
    <% if controller_name == 'registrations' %>
      <li class="active"><%= link_to t('devise.signup'), new_user_registration_path, class: 'menulink' %></li>
    <% else %>
      <li><%= link_to t('devise.signup'), new_user_registration_path, class: 'menulink' %></li>
    <% end %>
  <% end %>
</ul>
