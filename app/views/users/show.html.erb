<div class="row">
  <div class="user">
    <ol class="breadcrumb">
      <li><a href="#">保险超市网</a></li>
      <li><a href="#">会员中心</a></li>
      <li class="active">会员中心首页</li>
    </ol>
  </div>
</div>

<div class="row">
  <div class='col-md-2'>
    <%= render partial: 'nav_panels', locals: { user: current_user } %>
  </div>
  <div class='col-md-10'>
    <%= render(partial: 'show_admin', locals: { user: @user }) if @user.has_role? :admin %>
    <%= render(partial: 'show_moderator', locals: { user: @user }) if @user.has_role? :moderator %>
    <%= render(partial: 'show_manager', locals: { user: @user }) if @user.has_role? :manager %>
  </div>
</div>
