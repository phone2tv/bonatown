<div class="row">
  <div class="user">
    <ol class="breadcrumb">
      <li><a href="#">保险超市网</a></li>
      <li><a href="#">会员中心</a></li>
      <li class="active">会员中心首页</li>
    </ol>
  </div>
</div>

<div class='row'>
  <div class='col-md-2'>
    <%= render partial: 'nav_panels', locals: { user: current_user } %>
  </div> <!-- col-md-2 -->
  <div class='col-md-6'>
    <%= render(partial: 'form_edit_admin_profile', locals: { user: @user } ) if @user.has_role? :admin %>
    <%= render(partial: 'form_edit_moderator_profile', locals: { user: @user } ) if @user.has_role? :moderator %>
    <%= render(partial: 'form_edit_manager_profile', locals: { user: @user } ) if @user.has_role? :manager %>
    <%= render(partial: 'form_edit_quoter_profile', locals: { user: @user } ) if @user.has_role? :quoter %>
    <%= render(partial: 'form_edit_park_profile', locals: { user: @user } ) if @user.has_role? :park %>
    <%= render(partial: 'form_edit_customer_profile', locals: { user: @user } ) if @user.has_role? :customer %>
    <%= debug @profile %>
  </div>
</div> <!-- .row -->
